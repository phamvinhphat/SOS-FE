module.exports = {
    project: {
        ios: {},
        android: {}, // grouped into "project"
    },
    assets: ['./src/assets/images/*', './src/assets/fonts', './src/layouts/**/assets/*'], // stays the same
    // commands: require('./path-to-commands.js'), // formerly "plugin", returns an array of commands
};
