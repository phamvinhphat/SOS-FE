import React from 'react';
import { ImageStyle } from 'react-native';
import { Icon } from '@ui-kitten/components';

export const EmailIcon = (style?: ImageStyle) => <Icon {...style} name="email" />;
