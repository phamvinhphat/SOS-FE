import React from 'react';
import ViewHistoryHelper from '../../layouts/detail-accident/detai-history';

export const HelperHistoryScreen = ({ navigation }: any): React.ReactElement => (
    <ViewHistoryHelper navigation={navigation} />
);
