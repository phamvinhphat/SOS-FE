import React from 'react';
import ChangePassword from '../../layouts/users/change-password';

export const ChangePasswordScreen = ({ navigation }: any): React.ReactElement => (
    <ChangePassword navigation={navigation} />
);
