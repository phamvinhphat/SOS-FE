import React from 'react';
import ViewHistoryAccident from '../../layouts/accidents/history-accident';

export const AccidentHistoryScreen = ({ navigation }: any): React.ReactElement => (
    <ViewHistoryAccident navigation={navigation} />
);
