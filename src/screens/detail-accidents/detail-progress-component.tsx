import React from 'react';
import DetailAccidentProgress from '../../layouts/detail-accident/detail-Progress';

export const DetailProgressScreen = ({ navigation }: any): React.ReactElement => (
    <DetailAccidentProgress navigation={navigation} />
);
